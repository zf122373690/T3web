# T3 Web Clean Rebuild

当前目录是重写后的干净版本。旧代码已归档到 `legacy_current_code/`，只作为参考。

默认登录：

```text
admin
admin123
```

开发启动：

```powershell
.\start.bat
```

手动构建前端：

```powershell
cd frontend
npm install
npm run build
```
